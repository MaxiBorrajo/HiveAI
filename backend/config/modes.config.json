[
  {
    "name": "fast",
    "isCurrent": false,
    "description": "Prioritizes inference speed over output fidelity",
    "parameters": [
      {
        "name": "k_cache_type",
        "description": "Quantization type for the Key (K) cache in attention layers. Lower bit precision reduces VRAM and memory bandwidth usage at a minor cost to precision.",
        "type": "string",
        "defaultValue": "q4_0",
        "currentValue": "q4_0",
        "options": ["q8_0", "q4_0"],
        "requiresReload": true
      },
      {
        "name": "v_cache_type",
        "description": "Quantization type for the Value (V) cache in attention layers. Lower bit precision reduces memory footprint during token generation.",
        "type": "string",
        "defaultValue": "f16",
        "currentValue": "f16",
        "options": ["f16", "q8_0"],
        "requiresReload": true
      },
      {
        "name": "n_ctx",
        "description": "Context window size in tokens, defining the maximum prompt and history length the model can process simultaneously.",
        "type": "number",
        "defaultValue": 2048,
        "currentValue": 2048,
        "minValue": 512,
        "maxValue": 4096,
        "requiresReload": true
      },
      {
        "name": "n_gpu_layers",
        "description": "Number of model layers to offload to the GPU for hardware acceleration. Set to -1 to offload all layers.",
        "type": "number",
        "defaultValue": -1,
        "currentValue": -1,
        "minValue": -1,
        "maxValue": null,
        "note": "maxValue calculated at runtime based on the layers of the loaded model",
        "requiresReload": true
      },
      {
        "name": "n_batch",
        "description": "Batch size for prompt processing (prefill). Higher values process input prompts faster but consume more memory during ingestion.",
        "type": "number",
        "defaultValue": 1024,
        "currentValue": 1024,
        "minValue": 1,
        "maxValue": 4096,
        "requiresReload": true
      },
      {
        "name": "n_threads",
        "description": "Number of CPU threads to allocate for computation during inference.",
        "type": "number",
        "defaultValue": null,
        "currentValue": null,
        "note": "default = physical cores detected at runtime",
        "minValue": 1,
        "maxValue": null,
        "requiresReload": true
      },
      {
        "name": "mmap",
        "description": "Enables memory-mapping to load the model file directly from disk, reducing startup time and allowing the OS to page weights dynamically.",
        "type": "boolean",
        "defaultValue": false,
        "currentValue": false,
        "requiresReload": true
      },
      {
        "name": "mlock",
        "description": "Locks model weights in physical RAM to prevent the operating system from paging memory to swap disk, ensuring stable latency.",
        "type": "boolean",
        "defaultValue": true,
        "currentValue": true,
        "note": "true only if sufficient free RAM is available, validated at runtime",
        "requiresReload": true
      },
      {
        "name": "temperature",
        "description": "Controls randomness in token selection. Lower values make output more deterministic and focused; higher values produce more diverse and creative responses.",
        "type": "number",
        "defaultValue": 0.7,
        "currentValue": 0.7,
        "minValue": 0.0,
        "maxValue": 2.0,
        "requiresReload": false
      },
      {
        "name": "top_k",
        "description": "Limits token selection to the K highest-probability tokens at each step, filtering out unlikely choices.",
        "type": "number",
        "defaultValue": 40,
        "currentValue": 40,
        "minValue": 0,
        "maxValue": 100,
        "requiresReload": false
      },
      {
        "name": "top_p",
        "description": "Nucleus sampling probability threshold, restricting generation to candidate tokens whose cumulative probability reaches P.",
        "type": "number",
        "defaultValue": 0.9,
        "currentValue": 0.9,
        "minValue": 0.0,
        "maxValue": 1.0,
        "requiresReload": false
      },
      {
        "name": "repeat_penalty",
        "description": "Applies a penalty to recently generated tokens to discourage the model from repeating words, phrases, or looping.",
        "type": "number",
        "defaultValue": 1.1,
        "currentValue": 1.1,
        "minValue": 1.0,
        "maxValue": 1.5,
        "requiresReload": false
      },
      {
        "name": "limit_output",
        "description": "Dynamically limits maximum generation tokens based on available context and task type.",
        "type": "boolean",
        "defaultValue": true,
        "currentValue": true,
        "note": "if true, num_predict is calculated at runtime based on available n_ctx and task type; if false, num_predict = -1 (unlimited, stopped by EOS)",
        "requiresReload": false
      },
      {
        "name": "spec_deco",
        "description": "Enables speculative decoding using a smaller draft model to accelerate token generation without sacrificing primary model accuracy.",
        "type": "string",
        "defaultValue": "",
        "currentValue": "",
        "options": [],
        "note": "options populated at runtime with compatible models (same family/tokenizer as the main model) already downloaded by the user; empty string = disabled",
        "requiresReload": true
      }
    ]
  },
  {
    "name": "quality",
    "isCurrent": false,
    "description": "Prioritizes output fidelity over inference speed",
    "parameters": [
      {
        "name": "k_cache_type",
        "description": "Quantization type for the Key (K) cache in attention layers. Lower bit precision reduces VRAM and memory bandwidth usage at a minor cost to precision.",
        "type": "string",
        "defaultValue": "f16",
        "currentValue": "f16",
        "options": ["f16", "q8_0"],
        "requiresReload": true
      },
      {
        "name": "v_cache_type",
        "description": "Quantization type for the Value (V) cache in attention layers. Lower bit precision reduces memory footprint during token generation.",
        "type": "string",
        "defaultValue": "f16",
        "currentValue": "f16",
        "options": ["f16"],
        "requiresReload": true
      },
      {
        "name": "n_ctx",
        "description": "Context window size in tokens, defining the maximum prompt and history length the model can process simultaneously.",
        "type": "number",
        "defaultValue": 8192,
        "currentValue": 8192,
        "minValue": 4096,
        "maxValue": 131072,
        "requiresReload": true
      },
      {
        "name": "n_gpu_layers",
        "description": "Number of model layers to offload to the GPU for hardware acceleration. Set to -1 to offload all layers.",
        "type": "number",
        "defaultValue": -1,
        "currentValue": -1,
        "minValue": -1,
        "maxValue": null,
        "note": "maxValue calculated at runtime based on the layers of the loaded model",
        "requiresReload": true
      },
      {
        "name": "n_batch",
        "description": "Batch size for prompt processing (prefill). Higher values process input prompts faster but consume more memory during ingestion.",
        "type": "number",
        "defaultValue": 256,
        "currentValue": 256,
        "minValue": 1,
        "maxValue": 4096,
        "requiresReload": true
      },
      {
        "name": "n_threads",
        "description": "Number of CPU threads to allocate for computation during inference.",
        "type": "number",
        "defaultValue": null,
        "currentValue": null,
        "note": "default = physical cores detected at runtime",
        "minValue": 1,
        "maxValue": null,
        "requiresReload": true
      },
      {
        "name": "mmap",
        "description": "Enables memory-mapping to load the model file directly from disk, reducing startup time and allowing the OS to page weights dynamically.",
        "type": "boolean",
        "defaultValue": false,
        "currentValue": false,
        "requiresReload": true
      },
      {
        "name": "mlock",
        "description": "Locks model weights in physical RAM to prevent the operating system from paging memory to swap disk, ensuring stable latency.",
        "type": "boolean",
        "defaultValue": true,
        "currentValue": true,
        "note": "true only if sufficient free RAM is available, validated at runtime",
        "requiresReload": true
      },
      {
        "name": "temperature",
        "description": "Controls randomness in token selection. Lower values make output more deterministic and focused; higher values produce more diverse and creative responses.",
        "type": "number",
        "defaultValue": 0.6,
        "currentValue": 0.6,
        "minValue": 0.0,
        "maxValue": 2.0,
        "requiresReload": false
      },
      {
        "name": "top_k",
        "description": "Limits token selection to the K highest-probability tokens at each step, filtering out unlikely choices.",
        "type": "number",
        "defaultValue": 40,
        "currentValue": 40,
        "minValue": 0,
        "maxValue": 100,
        "requiresReload": false
      },
      {
        "name": "top_p",
        "description": "Nucleus sampling probability threshold, restricting generation to candidate tokens whose cumulative probability reaches P.",
        "type": "number",
        "defaultValue": 0.95,
        "currentValue": 0.95,
        "minValue": 0.0,
        "maxValue": 1.0,
        "requiresReload": false
      },
      {
        "name": "repeat_penalty",
        "description": "Applies a penalty to recently generated tokens to discourage the model from repeating words, phrases, or looping.",
        "type": "number",
        "defaultValue": 1.1,
        "currentValue": 1.1,
        "minValue": 1.0,
        "maxValue": 1.5,
        "requiresReload": false
      },
      {
        "name": "limit_output",
        "description": "Dynamically limits maximum generation tokens based on available context and task type.",
        "type": "boolean",
        "defaultValue": true,
        "currentValue": true,
        "note": "if true, num_predict is calculated at runtime based on available n_ctx and task type; if false, num_predict = -1 (unlimited, stopped by EOS)",
        "requiresReload": false
      },
      {
        "name": "spec_deco",
        "description": "Enables speculative decoding using a smaller draft model to accelerate token generation without sacrificing primary model accuracy.",
        "type": "string",
        "defaultValue": "",
        "currentValue": "",
        "options": [],
        "note": "options populated at runtime with compatible models (same family/tokenizer as the main model) already downloaded by the user; empty string = disabled",
        "requiresReload": true
      }
    ]
  },
  {
    "name": "custom",
    "isCurrent": false,
    "description": "Full manual control over all inference and model parameters",
    "parameters": [
      {
        "name": "k_cache_type",
        "description": "Quantization type for the Key (K) cache in attention layers. Lower bit precision reduces VRAM and memory bandwidth usage at a minor cost to precision.",
        "type": "string",
        "defaultValue": "q8_0",
        "currentValue": "q8_0",
        "options": ["f16", "q8_0", "q4_0"],
        "requiresReload": true
      },
      {
        "name": "v_cache_type",
        "description": "Quantization type for the Value (V) cache in attention layers. Lower bit precision reduces memory footprint during token generation.",
        "type": "string",
        "defaultValue": "f16",
        "currentValue": "f16",
        "options": ["f16", "q8_0", "q4_0"],
        "requiresReload": true
      },
      {
        "name": "n_ctx",
        "description": "Context window size in tokens, defining the maximum prompt and history length the model can process simultaneously.",
        "type": "number",
        "defaultValue": 4096,
        "currentValue": 4096,
        "minValue": 512,
        "maxValue": 131072,
        "requiresReload": true
      },
      {
        "name": "n_gpu_layers",
        "description": "Number of model layers to offload to the GPU for hardware acceleration. Set to -1 to offload all layers.",
        "type": "number",
        "defaultValue": -1,
        "currentValue": -1,
        "minValue": -1,
        "maxValue": null,
        "note": "maxValue calculated at runtime based on the layers of the loaded model",
        "requiresReload": true
      },
      {
        "name": "n_batch",
        "description": "Batch size for prompt processing (prefill). Higher values process input prompts faster but consume more memory during ingestion.",
        "type": "number",
        "defaultValue": 512,
        "currentValue": 512,
        "minValue": 1,
        "maxValue": 4096,
        "requiresReload": true
      },
      {
        "name": "n_threads",
        "description": "Number of CPU threads to allocate for computation during inference.",
        "type": "number",
        "defaultValue": null,
        "currentValue": null,
        "note": "default = physical cores detected at runtime, manually editable",
        "minValue": 1,
        "maxValue": null,
        "requiresReload": true
      },
      {
        "name": "mmap",
        "description": "Enables memory-mapping to load the model file directly from disk, reducing startup time and allowing the OS to page weights dynamically.",
        "type": "boolean",
        "defaultValue": true,
        "currentValue": true,
        "requiresReload": true
      },
      {
        "name": "mlock",
        "description": "Locks model weights in physical RAM to prevent the operating system from paging memory to swap disk, ensuring stable latency.",
        "type": "boolean",
        "defaultValue": false,
        "currentValue": false,
        "requiresReload": true
      },
      {
        "name": "temperature",
        "description": "Controls randomness in token selection. Lower values make output more deterministic and focused; higher values produce more diverse and creative responses.",
        "type": "number",
        "defaultValue": 0.7,
        "currentValue": 0.7,
        "minValue": 0.0,
        "maxValue": 2.0,
        "requiresReload": false
      },
      {
        "name": "top_k",
        "description": "Limits token selection to the K highest-probability tokens at each step, filtering out unlikely choices.",
        "type": "number",
        "defaultValue": 40,
        "currentValue": 40,
        "minValue": 0,
        "maxValue": 100,
        "requiresReload": false
      },
      {
        "name": "top_p",
        "description": "Nucleus sampling probability threshold, restricting generation to candidate tokens whose cumulative probability reaches P.",
        "type": "number",
        "defaultValue": 0.9,
        "currentValue": 0.9,
        "minValue": 0.0,
        "maxValue": 1.0,
        "requiresReload": false
      },
      {
        "name": "repeat_penalty",
        "description": "Applies a penalty to recently generated tokens to discourage the model from repeating words, phrases, or looping.",
        "type": "number",
        "defaultValue": 1.1,
        "currentValue": 1.1,
        "minValue": 1.0,
        "maxValue": 1.5,
        "requiresReload": false
      },
      {
        "name": "limit_output",
        "description": "Dynamically limits maximum generation tokens based on available context and task type.",
        "type": "boolean",
        "defaultValue": true,
        "currentValue": true,
        "note": "if true, num_predict is calculated at runtime based on available n_ctx and task type; if false, num_predict = -1 (unlimited, stopped by EOS)",
        "requiresReload": false
      },
      {
        "name": "spec_deco",
        "description": "Enables speculative decoding using a smaller draft model to accelerate token generation without sacrificing primary model accuracy.",
        "type": "string",
        "defaultValue": "",
        "currentValue": "",
        "options": [],
        "note": "options populated at runtime with compatible models (same family/tokenizer as the main model) already downloaded by the user; empty string = disabled",
        "requiresReload": true
      }
    ]
  },
  {
    "name": "default",
    "isCurrent": true,
    "description": "Balanced profile offering a trade-off between inference speed and output quality"
  }
]
